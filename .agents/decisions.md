# 확정된 결정사항

> 이 문서는 프로젝트에서 논의를 거쳐 **확정된 모든 결정**의 원본 기록입니다.
> "왜 이렇게 했지?"에 대한 답이 여기 있습니다.

---

## 결정 이력

### 2026-06-20 — 초기 기획 확정

| # | 항목 | 결정 | 근거 |
|---|------|------|------|
| D1 | 디자인 톤 | **모던 클린** — 블루/퍼플 그라데이션, 다크모드 지원 | 보안 전용이 아닌 다양한 주제를 포괄하는 블로그. 특정 분야에 치우치지 않는 깔끔한 디자인 |
| D2 | 폰트 | Pretendard(한글) + Inter(영문) + JetBrains Mono(코드) | AI 재량으로 선택. 가독성과 모던함의 균형 |
| D3 | 로고/파비콘 | 텍스트 로고 기반 + 심볼 파비콘 | AI 재량으로 선택 |
| D4 | 한/영 전환 범위 | **전체 UI** (헤더, 푸터, 검색, 버튼 등 모두) | 해외 채용·대외 제출 시 일관된 영문 경험 제공 |
| D5 | 검색 인덱스 생성 | **빌드 스크립트** (Node.js)로 자동 생성 | 수동 관리 시 누락 위험, 자동화가 안전 |
| D6 | 난이도 체계 | **5단계** (★☆☆☆☆ ~ ★★★★★), 주관적 기준 | 작성자가 체감 난이도로 자유롭게 부여 |
| D7 | 공통 컴포넌트 방식 | **JS fetch() 동적 삽입** (header.html / footer.html) | 프레임워크 없이 재사용 가능. 수정 시 한 곳만 변경 |
| D8 | 초기 콘텐츠 | **빈 껍데기** (구조 + 디자인만) | 콘텐츠는 추후 추가, 먼저 완성된 구조를 확보 |
| D9 | Travel 지도 | **Leaflet.js + OpenStreetMap** | 무료, 추가 API 키 불필요. 샘플 핀 포함 데모 |
| D10 | 폴더 구조 | `notes` → **`studies`** 로 변경 | 사용자 요청. "공부 노트"라는 의미를 더 잘 전달 |
| D11 | 구현 순서 | Phase 1→2→3→4→5 | 전역 레이아웃 → Studies → Projects → Travel → 전역 기능 |

### 2026-06-20 — 개발 프로세스 확정

| # | 항목 | 결정 | 근거 |
|---|------|------|------|
| D12 | 개발 방식 | **TDD (Test-Driven Development)** | 사용자 요청. 유닛 테스트 + 엣지 케이스까지 꼼꼼히 |
| D13 | 문서 관리 | `.agents` 폴더에 관심사별 분리 | 여러 사람이 작업 시 해당 문서만 참고하면 되도록 |

### 2026-07-02 — 다중 작업자 협업 프로세스 확정

> 이 시점부터 여러 사람 + 여러 AI 에이전트가 동시에 저장소를 건드리기 시작함. 그 결과 D1(블루/퍼플 테마)이 실제 코드(우드&베이지)와 어긋난 채 방치되는 등, "누가 뭘 바꿨는지 서로 모르는" 문제가 실제로 발생함.

| # | 항목 | 결정 | 근거 |
|---|------|------|------|
| D14 | `.agents` 폴더 공개 여부 | **gitignore 해제, git 추적 대상으로 전환** | 사용자 요청. 내용 확인 결과 이메일/키/개인정보 등 민감 정보 없음 확인됨. 여러 작업자가 같은 문서를 봐야 충돌을 줄일 수 있음 |
| D15 | 진행상황 기록 위치 | decisions.md(영구 결정)와 분리하여 **progress.md**(임시/유동적 현황판)에 기록 | 결정 이력과 "지금 어디까지 했다"가 한 문서에 섞이면 금방 지저분해지고 신뢰할 수 없게 됨 |
| D16 | 카테고리별(분야별) 시각 구분 방식 | 전역 테마(우드&베이지)는 고정, **뱃지 액센트 컬러만** 카테고리마다 다르게 — [styles.md](styles.md) 레지스트리 표로 관리 | CS 외 분야(금융인공지능 등)가 추가돼도 사이트 톤 일관성 유지. 새 카테고리는 반드시 표에 먼저 등록 후 사용 |

### 2026-07-02 — Finance(IE412 AI for Finance) 스터디노트 11편 구축

| # | 항목 | 결정 | 근거 |
|---|------|------|------|
| D17 | 수식 렌더링 라이브러리 | `plugins.md`에 등록된 **KaTeX 0.16.8**을 그대로 재사용 (버전 통일) | 새 라이브러리를 추가하지 않고 기존 등록 자산을 재사용해 관리 포인트를 늘리지 않음 |
| D18 | KaTeX 수식 구분자 | 인라인은 `\(...\)`, 블록은 `$$...$$`만 사용. 단일 `$...$`는 사용하지 않음 | Finance 노트 특성상 본문에 "$100" 같은 통화 표기가 매우 잦아, 단일 `$` 구분자를 쓰면 통화 표기와 충돌 위험이 큼(KaTeX 공식 문서도 이 경우 `\(...\)` 사용을 권장). 마크다운 소스에서는 `\\(`, `\\)`처럼 백슬래시를 두 번 써야 CommonMark 이스케이프 규칙을 통과해 최종 HTML에 `\(`, `\)`가 정확히 남는다는 점도 확인함 |
| D19 | 데이터 시각화 라이브러리 버전 | `plugins.md`에는 Chart.js 버전이 미고정 상태였으나, Finance 노트에서 실제 사용하며 **Chart.js 4.4.4로 고정** | 버전 미고정은 예고 없는 breaking change에 취약함(progress.md에 리스크로 기록되어 있었음). 실사용 시점에 고정하는 것으로 해소 |
| D20 | 글로벌 CSS 변수 누락 버그 수정 | `global.css`의 `:root`에 `--surface-*`, `--primary-color`, `--spacing-*`, `--border-color`, `--text-muted/primary/secondary`, `--font-size-*` 등 16개 호환 별칭을 **추가**(기존 규칙은 변경하지 않음) | `components.css`/`studies.css`가 참조하는 변수들이 애초에 `global.css`에 정의된 적이 없었던 사이트 전역 버그(Finance 작업 중 발견). 뱃지·TL;DR박스·카드 등 대부분의 UI 컴포넌트에 영향을 미치므로 카테고리 구분 없이 순수 추가 방식으로 즉시 수정. crypto 등 다른 카테고리 노트의 렌더링도 함께 개선됨 |
| D21 | Finance 노트 템플릿 | 기존 `note-template.html`(마크다운+`loadMarkdown()`) 패턴을 그대로 계승하되, `finance-notes.css`(전용 스타일시트: 선수지식 배지, 핵심 용어 미니 사전, 차트 컨테이너, 이전/다음 네비게이션)를 추가로 로드 | crypto 쪽의 `course.css` 3단 레이아웃과는 별개의 독립 스타일. progress.md의 "카테고리별 담당 경로 분리" 원칙에 따라 서로 다른 카테고리의 템플릿 실험이 충돌하지 않도록 함 |

### 2026-07-16 — Finance 노트 10편 확장 (교재 전체 커버리지) 및 사이트 전역 버그 수정

> 사용자가 "강의 PPT는 교재의 선물·옵션 파트를 건너뛰었다"고 지적하며 다루지 않은 내용을 파악해 보강하라고 요청. 추가로 공식 강의계획서(Week 7)에는 있었지만 노트가 없던 진짜 커리큘럼 공백(Ch8 팩터 모델/APT)도 발견됨. AskUserQuestion으로 범위를 확인한 결과 사용자가 "교재 전체 커버"를 선택해, 기존 11편에 10편을 추가로 삽입해 총 21편 시리즈로 확장.

| # | 항목 | 결정 | 근거 |
|---|------|------|------|
| D22 | 확장 범위 | Ch5(응용 이자율 분석), Ch8(팩터 모델/APT), Ch9 후반(상태가격/위험중립가격결정), Ch10~16(선도·선물·스왑, 이항격자, 옵션, 블랙-숄즈, 이자율파생상품, 최적성장이론, 일반투자평가) 총 10편을 신설 | 사용자가 AskUserQuestion에서 "교재 전체 커버" 옵션을 명시적으로 선택. Ch8만 공식 커리큘럼 공백이고 나머지는 사용자가 지적한 파생상품 내용 + 그 이해에 필요한 선행 이론(위험중립가격결정 등)을 포함한 확장 |
| D23 | 신규 노트 삽입 위치 | 기존 11편의 순서를 유지한 채, 5편(기간구조) 뒤에 Ch5를, 9편(CAPM) 뒤에 Ch8~16을 끼워 넣어 21편 전체 재배열. 기존 노트들의 prev/next pagination href·순번(x/21) 및 prereq-box 순번도 전부 갱신 | Luenberger 교재의 챕터 순서 및 강의계획서상 위상수학적 선후관계(위험중립가격결정 → 파생상품)를 그대로 반영 |
| D24 | 애니메이션 라이브러리 | 신규 도입 없이 `plugins.md`에 이미 등록된 **Anime.js 3.2.1**(crypto 카테고리가 먼저 도입)을 재사용. 이항격자 확장(13편), 실제확률→위험중립확률 전환(11편), 델타헤지 리밸런싱(15편) 3곳에만 제한적으로 사용 | 사용자가 "단순 차트로 설명 안 되는 개념에 한해 애니메이션 플러그인 사용 가능"이라고 명시적으로 범위를 한정함. 기존 등록 자산 재사용으로 관리 포인트 증가 없음 |
| D25 | 사이트 전역 버그: `.reading-progress` CSS 클래스 충돌 | `global.css`(standalone 패턴: `position:fixed; height:3px`)와 `studies.css`(container+fill 패턴: `height:100%`)가 동일 클래스명을 다른 용도로 중복 정의해 스크롤 시 그라데이션 패널이 화면 전체를 덮는 버그 발생(전 카테고리 공통, 14개 파일 영향). `studies.css` 쪽 규칙을 `.reading-progress-container .reading-progress`로 스코프하고 `position/top/left/z-index`를 명시적으로 재설정하는 방식으로 수정(`global.css`는 다른 용도로 쓰일 수 있어 그대로 둠) | 사용자의 긴급 버그 리포트("스크롤 시 그라데이션이 화면을 덮는다")에 대응. CSS 캐스케이드가 속성 단위로 병합된다는 점 때문에 두 스타일시트의 부분집합 규칙이 의도치 않게 합쳐진 것이 근본 원인 |

---

## 결정 변경 절차

1. 변경이 필요한 결정 항목을 식별
2. 변경 사유를 명시
3. 사용자 승인 후 이 문서를 업데이트
4. 관련 문서(styles.md, architecture.md 등)도 함께 업데이트
5. 변경 이력에 날짜와 함께 기록
