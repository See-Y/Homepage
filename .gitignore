# 의존성 모듈 (가장 중요)
node_modules/

# 로그 파일
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# Playwright (E2E 테스트 결과 및 리포트)
playwright-report/
test-results/
.playwright/

# 시스템 생성 파일
# .agents/ 는 2026-07-02부로 추적 대상 (팀 협업 문서로 공개, progress.md 참고)
.DS_Store

# 환경 변수
.env
.env.local

# 빌드 결과물 (로컬에서 생성된 검색 데이터 및 피드 등은 GitHub 대신 CI에서 생성되도록 할 수 있음)
# assets/data/search-index.json
# rss.xml

# 테스트 코드 및 설정 파일 (블로그 운영에 불필요)
tests/
jest.config.js
playwright.config.js
blog-plan.md

# 원본 데이터
data/